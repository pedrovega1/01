import * as THREE from 'three';

/* =========================================================
   ВСЕ НАСТРОЙКИ ПРОЕКТА В ОДНОМ МЕСТЕ
   Меняй значения тут — по коду искать не нужно.
   ========================================================= */

// ---- PS1-стиль ----
export const PS1 = {
  PIXEL: 1.3,   // рендерим в N раз меньше, CSS растягивает обратно (меньше = чётче)
  SNAP: 110.0,  // грубость сетки привязки вершин (меньше = сильнее дрожь)
};

// ---- пути к ассетам ----
export const ASSETS = {
  table: 'assets/Table.glb',
  pc: 'assets/pc.glb',
  camera: 'assets/Camera.glb',
  boombox: 'assets/cc0_free_low_poly_boombox.glb',
  door: 'assets/door_wooden_old_-9mb.glb',
  dancer: 'assets/dancer.fbx',
  // фото для комнат памяти
  photo1: 'assets/1.png',
  photo2: 'assets/2.png',
  photo3: 'assets/3.png',
  photo4: 'assets/4.png',
};

// ---- сцена со столом (фаза 1–2: камера едет в монитор) ----
// ВНИМАНИЕ: эти векторы доуточняются после загрузки pc.glb (по реальному экрану модели).
export const DESK = {
  PC_ROT_Y: 0,                                       // поверни на Math.PI, если монитор смотрит от камеры
  SCREEN_CENTER: new THREE.Vector3(0, 1.68, -1.42),  // центр экрана монитора
  CAM_START: new THREE.Vector3(0, 1.7, 4.2),         // старт камеры (видно всю комнату)
  CAM_END: new THREE.Vector3(0, 1.68, -1.18),        // финиш (вплотную перед экраном)
  SUN_DIR: new THREE.Vector3(0.02, 0.04, -1).normalize(), // направление на солнце (низко за столом)
};

// ---- бумбокс на столе ----
export const BOOMBOX = {
  WIDTH: 0.6,                                  // целевая ширина модели (масштаб считается от неё)
  position: new THREE.Vector3(-0.85, 0, -1.9), // слева от монитора (Y садится на столешницу автоматически)
  rotationY: 0.4,
};

// ---- танцор (Mixamo FBX) ----
export const DANCER = {
  HEIGHT: 1.9,                                 // целевой рост в метрах (масштаб считается от него)
  position: new THREE.Vector3(-2.9, 0, -3),  // на полу, позади видеокамеры
  rotationY: 0.65,                             // доворот лицом к камере зрителя
  PS1: false,                                  // PS1-стиль (дрожь вершин + гранёность). false = гладкий, меньше «пикселей»
};

// ---- видеокамера-реквизит ----
export const PROP_CAMERA = {
  position: new THREE.Vector3(-1.9, -0.2, -3.0),
  rotationY: 0.9,
};

// ---- комнаты памяти ----
export const ROOM = {
  W: 8, H: 4, D: 8,        // ширина/высота/глубина
  DOOR_H: 2.4,             // высота двери
  DOOR_OPEN: -1.5,         // угол открытия полотна (рад)
  PHOTO_Y: 2.0,            // высота центра фоторамок
  EPS: 0.06,               // отступ фото от стены
  MARGIN: 0.6,             // отступ камеры от стен при ходьбе
  START_Z: 3.2,            // глубина спавна в комнате 1 (лицом к двери/фото)
  START_Z_ROOM2: 1.4,      // в комнате 2 дверь у стены +z -> спавним ближе к фото-стене
};
